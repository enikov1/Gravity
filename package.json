{
  "name": "gravity-moto-trials",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Локальная HTML5-игра в жанре мото-триал",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.typecheck.json --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "serve": "npm run dev",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "matter-js": "^0.20.0",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/matter-js": "^0.20.2",
    "typescript": "^5.9.3",
    "vite": "^8.3.0"
  }
}
